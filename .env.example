# Шаблон. up.sh копирует его в .env при первом запуске (.env в git НЕ попадает).
# Значения читает docker compose для сервиса postgres.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me-please   # ← обязательно поменяйте на свой секрет
POSTGRES_DB=postgres
